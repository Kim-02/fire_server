# 상위 개념(구조), 보조 개념(지붕/외피)로 분리 태깅
RULES_COMPOSITE = [
    # 비닐하우스 계열
    (r"(비닐하우스\s*파이프조)",                 ["구조:비닐하우스파이프조"]),
    (r"(비닐하우스)",                           ["구조:비닐하우스"]),
    # 컨테이너
    (r"(컨테이너조\s*컨테이너|컨테이너조|컨테이너)", ["구조:컨테이너조"]),
    # 샌드위치패널 계열(조립식 포함)
    (r"(조립식\s*샌드위치패널조)|(샌드위치패널조)",   ["구조:샌드위치패널조"]),
    (r"(조립식\s*철골조\s*샌드위치패널)",            ["구조:철골조", "외피:샌드위치패널"]),
    (r"(철근콘크리트조\s*샌드위치패널)",            ["구조:철근콘크리트조", "외피:샌드위치패널"]),
    # SRC
    (r"(철골철근콘크리트조)",                     ["구조:SRC"]),
]

# ---- 3) 구조(상위) 단일 키워드
RULES_STRUCTURE = [
    (r"철근콘크리트조", "구조:철근콘크리트조"),
    (r"철골조",       "구조:철골조"),
    (r"벽돌조",       "구조:벽돌조"),
    (r"블록조",       "구조:블록조"),
    (r"목조",         "구조:목조"),
    (r"기타\s*조",    "구조:기타조"),
]

# ---- 4) 지붕/바닥/외피(보조) 키워드
RULES_ENVELOPE = [
    (r"슬라브가",        "지붕:슬라브"),
    (r"스레트가",        "지붕:슬레이트"),
    (r"시멘트기와",      "지붕:시멘트기와"),
    (r"한식기와|와가",   "지붕:기와"),
    (r"칼라피복철판",    "외피:칼라피복철판"),
    (r"샌드위치패널",    "외피:샌드위치패널"),
]

# ---- 5) 관용 오류/노이즈 정정(선택)
FIXUPS = [
    (r"기타\s*즙", "기타"),  # '즙' 오OCR -> '즉'/'측'류 오타 방지용 단순 치환
    (r"기타\s*\(건물구조즙코드\)", "기타"),
]